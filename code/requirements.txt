flask>=3.0.0
flask-socketio>=5.3.6
numpy>=1.26.0,<2  # Hailo C++ bindings (pyhailort) require numpy 1.x ABI
faster-whisper>=1.0.0
eventlet>=0.35.0
transformers>=4.36.0  # whisper-tiny tokenizer only — does NOT require torch
huggingface_hub>=0.20.0  # model download/cache (bundles hf-xet on 1.x)
hf_transfer>=0.1.6       # optional fast-download backend (HF_HUB_ENABLE_HF_TRANSFER=1)
requests>=2.31.0
ephem>=4.1.0
pyais>=2.6.0
Pillow>=10.0.0  # WEFAX decoder PNG output (ravensdr/wefax_decode.py)
